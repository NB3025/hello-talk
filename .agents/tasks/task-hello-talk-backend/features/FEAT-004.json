{
  "id": "FEAT-004",
  "type": "docs",
  "description": "End-to-end verification of the real client/server flow and documentation of how to run and test everything. Depends on FEAT-001..003. Proves the definition of done: a genuine cross-session flow works against the server (not localStorage), with documented commands. Also documents deployment posture (frontend stays on S3+CloudFront; backend + managed Postgres as a separate service) without inventing an undecided AWS compute service or applying Terraform (CLI not installed).",
  "status": "completed",
  "steps": [
    "Provide a single documented local dev sequence: start Postgres (server/scripts/db.sh), run backend migrations + `npm run dev` in server/, and `npm run dev` for the frontend with VITE_API_BASE pointed at the backend. Confirm the frontend loads against the live server.",
    "Run an end-to-end check of the real flow against the server (two browser sessions/tabs OR an automated HTTP+WS script driving two sessions): create user A and user B, A adds B by friend code, open a direct chat, A sends a message and B receives it via WebSocket with correct read-state (B's unread increments, clears on markRead, A sees 'read' after B reads), A sends a gift to B, B accepts (bubble flips to '사용중 · N일 남음' and ownership transfers), and run at least one further lifecycle action (e.g. use or extend, plus a rejected/authorization-failure case). Capture the exact commands/steps used and the observed results. If done via a script, place it under server/ or a scripts/ dir and document it.",
    "Update README.md (or add server/README.md) with: backend architecture summary (Fastify + Postgres + REST + WS, session-cookie auth, domain-code reuse, single-instance in-process WS fan-out, no Redis yet), the exact run commands (Postgres via docker run, backend, frontend, env vars), the test commands (frontend `npm test`; backend `npm test` with Postgres running), and the idempotency/authorization guarantees. Keep the existing localStorage demo-mode explanation but note the server-backed mode.",
    "Document deployment posture without fabricating: frontend continues on private S3 + CloudFront (existing infra-tf); the backend deploys as a separate service with separately provisioned managed PostgreSQL, isolated dev/prod, managed secrets, DB backups, logs, monitoring, health checks (GET /health exists). Explicitly note the concrete AWS backend compute service is not yet decided and that backend IaC is deferred (Terraform CLI not installed in this environment). Do NOT invent a specific compute service or write speculative backend Terraform.",
    "Final green pass: root `npm test && npm run build && npm run typecheck`; server `npm test && npm run typecheck && npm run build` with Postgres up. Confirm `git status` shows all intended changes staged/committed on the feature branch and that .kiro/ remains uncommitted."
  ],
  "acceptance_criteria": [
    "A documented, reproducible local setup brings up Postgres + backend + frontend and the frontend operates against the live server.",
    "The full real flow (create users, add friend, direct chat, message with read state, gift send + accept + one more lifecycle action + an authorization-rejection case) is verified against the server and the commands/results are recorded.",
    "README (root and/or server) documents run commands, test commands, env vars, and the architecture/guarantees; deployment posture is documented as deferred for backend compute without inventing a service.",
    "Both frontend and backend test/build/typecheck pass in a final combined run; .kiro/ is not committed."
  ],
  "verification": [
    "Root: `npm test && npm run build && npm run typecheck`.",
    "Server (Postgres running): `npm test && npm run typecheck && npm run build`.",
    "Execute the documented e2e sequence/script and confirm messages, read state, and gift lifecycle propagate via the server + WebSocket across two sessions.",
    "`git status` confirms feature branch has the changes committed and .kiro/ is untracked."
  ],
  "blocked_reason": null,
  "findings": "COMPLETED on branch feat/backend (committed alongside e2e + docs). The full cross-session flow is PROVEN against the REAL server + REAL PostgreSQL 15 + REAL WebSocket (not localStorage, not mocks).\n\nE2E SCRIPT (server/scripts/e2e.ts, run via `npm run e2e` in server/): boots the DB as a child process over a unix socket (same native-child harness as the vitest globalSetup — required because each execute_bash runs in an ephemeral bubblewrap PID+network namespace where cross-command loopback is EHOSTUNREACH and backgrounded servers are reaped), builds the Fastify app in-process, listens on a real ephemeral port, opens TWO real ws WebSocket clients (A and B) plus two cookie jars, drives the whole flow via real HTTP fetch + WS, asserts, and exits nonzero on failure. Last run: 52 assertions passed, exit 0.\n\nWHAT THE E2E PROVES (all against the live server): (1) create user A (홍길동) + B (김철수) with server-issued friend codes, distinct sessions/cookies; unauthenticated WS closed 1008. (2) A adds B by friend code -> A→B friendship exists, reverse B→A does NOT (directional), duplicate rejected with '이미 친구' reason, A receives WS 'friends' change. (3) openDirectChat idempotent: 2nd open from A and open from B both return the SAME chatId, exactly 1 direct room. (4) A sends message -> B receives WS 'messages' push; B unread=1, A's own=0; markRead clears B unread to 0 and advances B's lastReadAt as seen by A ('read'); no-op markRead emits nothing; retry with same clientKey does not duplicate. (5) gift: A sends gift to B -> gift order + gift message land atomically in the same chat, B gets WS 'gifts' push; ACCEPT by outsider C rejected ('당사자'), ACCEPT by wrong actor A rejected ('받는 사람'), state stays 'paid'; FORCE_EXPIRE rejected 400; B ACCEPT -> status accepted + 90-day voucher + ownership stays receiver B; EXTEND once -> ~180 days, 2nd EXTEND rejected (1-time limit); duplicate gift clientKey -> still 1 order.\n\nDOCS: root README.md gained a '서버 모드' section (local dev sequence with VITE_API_BASE + PORT/DATABASE_URL/COOKIE_SECRET/DEV_TOOLS env table, backend architecture summary: Fastify+PG+REST+WS, HttpOnly signed session-cookie as sole actor identity, domain-code reuse, single-instance in-process WS fan-out with NO Redis yet / Redis Pub/Sub as future path, idempotency + server-side authorization + FORCE_EXPIRE blocked + no prod reset + GET /health), updated the '두 사람이 대화' section to demo-mode, updated limitations to distinguish demo vs server mode, updated the test section (frontend 77, backend 41 self-contained DB, e2e command with the 52-assertion flow described + sandbox one-process rationale), and added a 'backend deployment (planned, not applied)' subsection: frontend stays on private S3+CloudFront; backend as a separate service with separately provisioned managed PostgreSQL, isolated dev/prod, managed secrets, DB backups, logs/monitoring, health checks; concrete AWS compute service explicitly NOT decided and backend IaC DEFERRED (Terraform CLI not installed) — no invented compute service, no speculative backend Terraform. server/README.md gained the e2e section + updated verification list.\n\nFINAL GREEN PASS: root `npm test` = 77 passed, `npm run build` ok, `npm run typecheck` ok. server `npm test` = 41 passed (self-contained native Postgres child), `npm run typecheck` ok, `npm run build` ok. `npm run e2e` exit 0 (52 assertions). git status: changes committed on feat/backend; .kiro/ remains untracked (not committed).\n\nNOTE: docs keep the human/local Docker Postgres path (server/scripts/db.sh, `npm run db:up`) AND document the sandbox native-child harness the tests/e2e use — the Docker path is for a real Docker daemon; in this sandbox the native-child-over-unix-socket approach is what actually runs (Docker Postgres is unreachable across command namespaces, per FEAT-002 findings)."
}
